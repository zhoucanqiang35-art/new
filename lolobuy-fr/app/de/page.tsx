import { LoloSite } from "../lolo-site";
import { localeMetadata } from "../locale-metadata";
export const metadata = localeMetadata.de;
export default function Page() { return <LoloSite locale="de" />; }
