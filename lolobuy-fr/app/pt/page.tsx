import { LoloSite } from "../lolo-site";
import { localeMetadata } from "../locale-metadata";
export const metadata = localeMetadata.pt;
export default function Page() { return <LoloSite locale="pt" />; }
