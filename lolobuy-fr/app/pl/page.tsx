import { LoloSite } from "../lolo-site";
import { localeMetadata } from "../locale-metadata";
export const metadata = localeMetadata.pl;
export default function Page() { return <LoloSite locale="pl" />; }
