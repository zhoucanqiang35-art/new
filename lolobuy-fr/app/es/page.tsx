import { LoloSite } from "../lolo-site";
import { localeMetadata } from "../locale-metadata";
export const metadata = localeMetadata.es;
export default function Page() { return <LoloSite locale="es" />; }
