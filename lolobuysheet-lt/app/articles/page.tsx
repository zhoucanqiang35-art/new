import type { Metadata } from "next";
import { PageHero, PageShell } from "../components";
import { researchArticles } from "../articleData";
import { languages } from "../data";
export const metadata: Metadata = { title: "LoloBuy Research Articles", description: "Source-grounded LoloBuy buying, QC, shipping and market research for Western buyers.", alternates: { canonical: "/articles", languages:Object.fromEntries([["x-default","/articles"],...languages.map(language=>[language.code,`/languages/${language.code}/articles`])]) } };
export default function ArticlesPage(){return <PageShell><div className="article-wrap"><PageHero eyebrow="Research library" title="Useful articles, built from checkable facts." intro="Each published guide is an independent 1,200–1,800 word article. Confirmed platform statements are kept separate from editorial checks and live variables."/><div className="library-grid">{researchArticles.map(article=><a key={article.slug} href={`/articles/${article.slug}`}><span>{article.tag} · {article.readTime}</span><h2>{article.title}</h2><p>{article.description}</p><b>Read full article →</b></a>)}</div><section className="research-block"><p className="eyebrow">Editorial standard</p><h2>No invented numbers or delivery promises</h2><p>The current official public material confirms purchasing support for Chinese online and offline channels, warehouse quality inspection with photos, parcel consolidation and international shipping. Where the public page does not establish a fixed photo count, storage period, route price or delivery time, these articles tell readers to verify the live account instead of repeating an unsupported number.</p></section></div></PageShell>}
