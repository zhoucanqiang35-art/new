# joyagooo-com

Joyagoo product-research website source.

## Build

Run `node generate.mjs` from this directory. It generates the complete static site into `dist/`.

The production host is configured in `.openai/hosting.json` to serve `dist`.
