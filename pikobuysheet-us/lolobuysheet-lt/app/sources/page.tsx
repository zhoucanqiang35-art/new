import type { Metadata } from "next";
import { PageHero, PageShell } from "../components";
import { sourceLinks } from "../data";
export const metadata: Metadata = { title: "Official Sources", description: "The official LoloBuy sources used for buying, QC, storage, shipping and restricted-item guidance.", alternates: { canonical: "/sources" } };
export default function SourcesPage(){return <PageShell><div className="article-wrap"><PageHero eyebrow="Evidence ledger" title="Primary sources before promotional claims." intro="This internal evidence ledger records what was checked and how each source is used. It deliberately avoids outbound links so every external click on this site supports FindSpreadsheet only."/><div className="source-table"><div className="source-table__head"><span>Evidence topic</span><span>Used for</span><span>Checked</span><span></span></div>{sourceLinks.map(source=><div id={source.id} className="source-table__row" key={source.id}><strong>{source.title}</strong><span>{source.label}</span><span>{source.checked}</span><b>✓</b></div>)}</div><section className="research-block"><p className="eyebrow">Research standard</p><h2>What we will not claim</h2><p>We do not call a seller “verified” without a documented test, turn an estimated delivery window into a guarantee, present a converted price as a checkout quote, or describe the official hot-region list as customer market share. When a fact can change, the page tells the reader what must be verified live.</p><a className="primary-action source-main-cta" href="https://findspreadsheet.com/" target="_blank" rel="noopener">Continue to FindSpreadsheet ↗</a></section></div></PageShell>}
