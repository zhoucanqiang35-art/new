import { LoloSite } from "../lolo-site";
import { localeMetadata } from "../locale-metadata";
export const metadata = localeMetadata.it;
export default function Page() { return <LoloSite locale="it" />; }
